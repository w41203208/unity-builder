pipeline {
  agent { node { label "Agent-1" } }
  stages {
    stage('Test') {
      steps {
        echo "Hello world"
        sh '''
          sudo rm -r temp
          
          sudo mkdir temp

          sudo rm -r test.log

          sh docker-cmd.sh build>>test.log
        '''

        // script {
          
        // }
      }
    }
  }
}