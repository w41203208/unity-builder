pipeline {
  agent { node { label "Agent-1" } }
  stages {
    stage('Test') {
      steps {
        echo "Hello world"
        sh '''
          if [ -e temp/ ]; then
            sudo rm -r temp;
          fi

          if [ -e build/ ]; then
            sudo rm -r build;
          fi

          sudo mkdir temp
          sudo mkdir build

          sudo rm -r test.log

          sh docker-cmd.sh build>>test.log
        '''

        // script {
          
        // }
      }
    }
  }
}